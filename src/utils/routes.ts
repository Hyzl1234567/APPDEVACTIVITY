interface RouteConstants {
  LOGIN: string;
  REGISTER: string;
  HOME: string;
  PROFILE: string;
  ERROR: string;
}

const routes: RouteConstants = {
  // Auth
  LOGIN: 'Login',
  REGISTER: 'Register',

  // Main
  HOME: 'Home',
  PROFILE: 'Profile',
  ERROR: 'Error',
};

export default routes;
